1. Services

    a. create a service classification (use inheritance and env for this)
    b. Pricing for services and create new products 
    c. service should be a superclass 
    d. all classifications of the service class will have an object, they will inherit the base functions from the Service class 
    e. ServiceController will call the env helper to differentiate the different type of classes and instantiate accordingly


2. Use Logger helper to generate proper logs when handling 

